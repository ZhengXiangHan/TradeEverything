# 顶层目录
TOP_DIR := $(shell cd ..;pwd)
# 编译器
GPP := g++
# 头文件路径
INCLUDE_PATH := $(TOP_DIR)/include
# 源文件路径
SOURCE_PATH := $(TOP_DIR)/source
# 输出的程序路径
BIN_PATH := $(TOP_DIR)/bin
# LIB库的路径
LIB_PATH := $(TOP_DIR)/lib
# JSON库路径
JSON_PATH := $(TOP_DIR)/json
